# neighborhood-watch

## Running the project

### Backend

1. Navigate to the backend folder
2. Create a new python venv (a good name would be "nwenv")
3. Activate the venv
4. Run `pip install -r requirements.txt` to get all dependencies